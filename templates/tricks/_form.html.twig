{{ form_start(form) }}
{{ form_widget(form) }}
<input
  type="submit"
  value="{{ submitButtonText|default('Create Trick') }}"
  class="btn btn-primary btn-block"
  formnovalidate
/>
{{ form_end(form) }}
